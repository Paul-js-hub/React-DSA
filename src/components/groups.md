- selected tags needs not to be reordered
selected tags then group them by the groupname
reorder the groups by the
- splice the length of that group 
- fields to move (fromindex+offset+newsstagslected.lenght)